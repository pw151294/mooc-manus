对 mooc-manus 项目两处文件进行代码解耦重构，遵循单一职责，剥离主流程内底层实现细节，仅保留主干业务逻辑，不改动原有业务逻辑与输出行为：
1. `internal/applications/services/agent.go` Chat 方法：
   把「规划提示词注入+断点续跑恢复」「rootSpan 链路初始化开启」两段前置逻辑分别封装为工具私有方法，Chat 仅调用方法，移除内嵌实现代码。
2. `internal/domains/services/agents/base.go`：
   ① StreamingInvokeLLM、InvokeLLM：将函数内所有 llmSpan 标签、日志、属性埋点链路追踪代码抽成独立工具函数，参考 injectInterventionIfNeeded 封装方式，原函数只保留LLM调用主干逻辑；
   ② StreamingInvoke、Invoke：抽离 rootSpan、roundSpan 所有链路追踪赋值与日志逻辑为工具函数，参考 injectEventChToSubagent 封装范式，主流程仅保留会话轮次调度核心逻辑；
   新增方法同文件内私有定义，保证重构前后功能、链路埋点、提示词能力完全等价，严格匹配项目现有代码规范。